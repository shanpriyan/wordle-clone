export const ROW_COUNT = 6;
export const COLUMN_COUNT = 5;

export const initialBoard = Array(ROW_COUNT).fill(Array(COLUMN_COUNT).fill(''));
export const initialTileState = Array(ROW_COUNT).fill(
  Array(COLUMN_COUNT).fill(''),
);
